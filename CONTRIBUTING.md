# 体験・プログラム制作ガイド

Copilot Experience Lab の体験やプログラムを追加・改訂するときの制作ルールです。

---

## 大原則

1. **体験の実体は `contents/` に一度だけ置く。** `programs/` にはコピーせず、相対リンクで参照します。
2. **情報は、それを必要とする読者の場所に一度だけ書く。** 他の場所からはリンクで参照します。
3. **件数・総数を本文に書かない。** コンテンツは増え続けるため、「全 ◯ 件」「（◯ 件）」のような固定値は使いません。
4. **実在する顧客の情報や機密情報を含めない。** 参加者自身の業務データを使う体験には、プライバシー上の注意を明記します。

---

## どこに何を書くか

| ファイル | 読者 | 役割 |
| --- | --- | --- |
| `README.md`（ルート） | 全員（初見） | ラボ全体の入口。読者を行き先へ振り分ける |
| `contents/README.md` | 体験を選ぶ人 | 体験の一覧（索引） |
| `programs/README.md` | プログラムを選ぶ人・組む人 | プログラムの一覧と選び方 |
| `programs/<program>/README.md` | 進行役 | そのプログラムの進行ガイド。使うサンプルデータもここに書く |
| `programs/<program>/index.md` | エンドユーザー | そのプログラムのエンドユーザー向けページ |
| `CONTRIBUTING.md` | 追加・改訂する人 | このファイル |

迷ったときは、**その情報を必要とする人が誰か**で置き場所を決めてください。

### 体験とプログラムの区別

- 1 つの目的を、利用者が手順に沿って実行する単位は「体験」とし、`contents/` に追加します。
- 複数の体験を対象者、順序、所要時間、必須・任意のルールとともに組み合わせたものは「プログラム」とし、`programs/` に追加します。
- 新しく作成する内容に、個別に実行できる手順と、それらを組み合わせた全体の進行が含まれる場合は、個々の手順を「体験」として `contents/` に分け、対象者や実施順序などの全体設計を「プログラム」として `programs/` に整理します。

---

## 体験を追加・改訂する

### 1. 体験 ID を決める

`<領域接頭辞>-<連番 2 桁>` の形式です。

| 接頭辞 | 領域 | 配置先 |
|---|---|---|
| `SETUP-` | 事前準備・セキュリティなど | `contents/00-setup/` |
| `CHAT-` | Copilot Chat | `contents/01-copilot-chat/` |
| `CHAT-IMG-` | Copilot Chat（画像生成） | `contents/01-copilot-chat/` |
| `MAL-` | メール | `contents/02-outlook-teams/` |
| `MTG-` | 会議シナリオ | `contents/02-outlook-teams/` |
| `CATCH-` | キャッチアップ シナリオ | `contents/02-outlook-teams/` |
| `XLS-` | Excel | `contents/03-excel/` |
| `WRD-` | Word | `contents/04-word/` |
| `PPT-` | PowerPoint | `contents/05-powerpoint/` |
| `AGT-` | Researcher / Analyst エージェント | `contents/06-researcher-analyst/` |
| `AGB-` | Agent Builder | `contents/07-agent-builder/` |
| `CWK-` | Copilot Cowork | `contents/08-copilot-cowork/` |

新しい ID を決める前に、`contents/` と `programs/` をリポジトリ全体で検索し、別の体験に割り当て済みでないことを確認してください。同じ領域では、原則として既存 ID の最大番号に 1 を加えます。

例：`CHAT-18` まで使用されている場合、次の体験は `CHAT-19` とします。

一度発行した ID は変更しません（プログラムからの参照が壊れるため）。
体験を廃止する場合も、ID を再利用せず、欠番のままにしてください。

新しい領域が必要になったら、この表に追加してください。

#### 新しいカテゴリを追加する場合

既存カテゴリに該当しない領域の体験を追加するときは、次の手順でカテゴリを作成します。

1. `contents/NN-category-name/` 形式のフォルダーを作成します。
2. `NN` には既存フォルダーと重複しない 2 桁の番号を使用し、`category-name` には英小文字、数字、ハイフンのみを使用します。
3. 「体験 ID を決める」の接頭辞表に、接頭辞、領域、配置先を追加します。
4. ルートの `index.md` に、カテゴリ名、フォルダー名、表示順を追加します。
5. `contents/README.md` にカテゴリと体験を追加します。
6. GitHub Pages でカテゴリ見出しと体験カードが正しく表示されることを確認します。

### 2. ファイル名を決める

`<体験ID>_<ハンズオンの内容>.md`

例：`XLS-01_売上データから地域別の弱点と価格施策を導く.md`

- **ファイル名だけで何を体験するか分かる**ように、動詞で終わる説明的な名前にする
- 抽象的な名前（「データ分析」「文書作成」）は避け、成果が想像できる粒度にする
- 体験 ID は先頭に置き、`_`（アンダースコア）で内容と区切る
- ファイル名の内容部分は、本文の H1 タイトルおよび `contents/README.md` の一覧の表記と一致させる

---

### 3. 本文を作る

体験コンテンツは次の形式で作成します。

````markdown
# タイトル｜XXX-00

| 項目 | 内容 |
|---|---|
| **目的** | |
| **所要** | |
| **利用** | |
| **入力** | **入力データの区分**：具体的な入力 |
| **成果** | |

## シナリオ
## TRY — 手順
## WATCH（動画など）
## REFLECT — 振り返り
## NEXT
````

- Front Matter は追加せず、ファイルの先頭を `# タイトル｜体験ID` 形式の H1 にする。この H1 は GitHub Pages の大きなページタイトルに使われ、本文側では重複表示されない。
- **TRY** のプロンプトは必ずコード ブロック（` ```text `）に入れる。参加者がそのままコピーできるようにするため。
- **WATCH** には `contents/assets/<体験ID>/` を参照するパスを書く。素材がない場合はセクションごとコメントアウトする。
- **REFLECT** は、その場で自分に答える 3 つの問いにする。
- **NEXT** は相対リンクで `contents/` 内の次の体験を指す。

#### 入力データの区分

体験で使用する入力データは、次の区分を使用して本文の「入力」欄の先頭に太字で記載します。区分と具体的な入力は、全角コロン（`：`）で区切って1行に収めます。この区分は、GitHub Pagesの体験カードにも自動表示されます。

- `自分の業務データ`: 利用者自身のメール、会議、ファイルなどを使用する
- `サンプル ファイル`: リポジトリまたは GitHub Release で提供するファイルを使用する
- `サンプル（プロンプト内）`: プロンプトに記載された架空のデータだけを使用する
- `公開情報`: 自社の公式Webサイトなど、公開されている情報を使用する

複数の区分に該当する場合は、該当する区分をすべて「入力」欄に併記してください。利用者が準備する順に記載し、補足が必要なデータやサンプル ファイルがある場合は、使用するファイル名や入手先を「入力」欄または手順内に明記します。

例：`**自分の業務データ**：自分がアクセスできる直近の会議`

複数の区分を使う例：`**自分の業務データ／サンプル ファイル**：自分の会議メモ、sales-data.xlsx`

`自分の業務データ` を使用する場合は、実在する顧客の情報、個人情報、機密情報を入力しないための注意事項を、手順の前に明記してください。

### 4. 一覧とプログラムを更新する

体験を追加したら、**[`contents/README.md`](contents/README.md) の一覧に 1 行追加してください。**
ID・体験名・カテゴリ・所要・利用・入力データを埋めます。

その体験をプログラムに組み込む場合は、該当プログラムの `README.md` と `index.md` の両方に追加し、**体験の並びと必須・選択・発展の区分を一致させてください。**

### 5. アセットを配置する

```text
contents/assets/<体験ID>/
├─ demo.gif
└─ sample-output.md
```

軽量なアセットは体験 ID のフォルダーで管理し、複数プログラムから同じアセットを参照します。

GitHub Pages と GitHub 上の Markdown の両方で自動再生するデモは、最適化した GIF を使用します。GitHub 上でも本文幅を満たすよう、横幅 1008px、8fps、128 色を目安にし、体験 ID のフォルダーへ配置してください。高画質な MP4 原本は Git 履歴へ追加せず、GitHub Releases に配置します。

```markdown
![実行例](../assets/<体験ID>/<体験ID>.gif)
```

#### サンプルデータ

体験で使うサンプルデータ一式は、`contents/assets/` 配下にデータセットごとのフォルダーを作って置きます。
配布用の一式は GitHub Releases に配置します。

**どのファイルを何に使うかは、それを使う体験の「入力」欄と、そのデータを使うプログラムの README に書きます。** 置き場所であるこのフォルダーには、一覧を持たせません。

---

## 新しいプログラムを追加する

1. [`contents/README.md`](contents/README.md) から、対象者と入力データに合う既存の体験を選ぶ
2. 既存の体験で足りない場合のみ、新しい体験を `contents/` に追加する
3. [`programs/templates/`](programs/templates/) の雛形をコピーして、`programs/<program-name>/README.md` を作成する
4. 対象者、時間、体験の順番、必須/選択、ゴールを定義する
5. サンプルデータを使う場合は、必要なファイルと配置先を README の事前準備に書く
6. エンドユーザー向けページが必要な場合は、同じフォルダーに `index.md` を作成する
7. [`programs/README.md`](programs/README.md) の一覧に追加する

### プログラムフォルダーの命名

プログラムのフォルダー名には、英小文字、数字、ハイフンのみを使用し、内容を短く表す kebab-case にします。空白、日本語、大文字、アンダースコアは使用しません。

例：`smb-guided-experience`

新しいフォルダーを作る前に、`programs/` 配下に同じ名前が存在しないことを確認してください。フォルダー名は GitHub Pages の URL に使用されるため、公開後は原則として変更しません。

### `README.md` と `index.md` の使い分け

- `README.md` は、パートナーや進行役がプログラムを準備・運営するための文書です。[`programs/templates/README.md`](programs/templates/README.md) を基準に、対象者、目的、所要時間、事前準備、進行方法、体験の順序、選択ルール、トラブル時の対応を記載します。
- `index.md` は、エンドユーザーがプログラムを理解し、各体験へ進むための GitHub Pages です。`README.md` の単純な転載にはせず、想定するエンドユーザーと利用場面に合わせて構成や表現を設計します。
- 同じ説明を両方へ重複させる必要はありません。ただし、所要時間、使用するデータ、体験の順序、必須・選択など、両方に記載する情報は一致させてください。

### エンドユーザー向け `index.md` の設計

想定する閲覧者や実施形式はプログラムごとに異なるため、ページ構成は一律に定めません。進行役から説明を受けなくても、エンドユーザーが次の内容を判断できるページにします。

- どのようなプログラムで、何を目指すのか
- どの程度の時間が必要か
- 始める前に何を準備し、どのデータを使用するか
- どの体験から始めるか、または何を基準に体験を選ぶか
- 各体験へどのように進むか

対象者を限定する場合は対象者を明記します。必須・選択・発展の区分、サンプルデータ、ストーリー、プログラム全体の振り返りなどは、そのプログラムで必要な場合に追加してください。

実装例として、`programs/` 配下にある既存の各プログラムの `index.md` を参照し、想定するエンドユーザーや実施形式が近いページの構成や表現を参考にしてください。
新しい `index.md` は、想定するエンドユーザーや実施形式が近い既存プログラムを基準とし、配色、余白、見出し、カード、ボタンなどのデザインと実装パターンを踏襲してください。独自デザインを採用する場合も、ルートページおよび既存プログラムとの視覚的な一貫性を保ってください。

### 元資料がある場合

プログラムの作成・改訂に元となる資料がある場合は、`README.md` の末尾に「参考資料」を設け、次の情報を記載します。元資料がない場合、この節は不要です。

- 資料名
- 公開 URL、公式の配布 URL、またはリポジトリ内の保管場所
- 参照した版、公開日、または参照日（確認できる場合）

社内限定 URL、顧客情報、機密情報などは記載しないでください。公式の配布 URL でサインインやアクセス権が必要な場合は、その条件を併記します。公開できる参照先がない場合は、機密情報を含まない資料名だけを記載します。

---

## 文章作成のルール

- 同じカテゴリの既存コンテンツを参照し、見出し構成、用語、文体、手順の詳しさをそろえる
- **業務担当者がそのまま読んで行動できる、簡潔で自然な日本語**で書く
- 元となる資料がある場合も直訳せず、意図を保ちながら、このリポジトリの構成と文体に合わせて書き直す
- 製品名・UI ラベルは、実際の日本語 UI の表記に合わせる（表記ゆれがある場合は「表示名は環境により異なる」と補足する）
- プロンプト本文は日本語で書く。ただし、架空の企業名や製品名などの固有名詞は正式な表記を維持する
- 架空の企業・人物であることが分かるようにする
- 所要時間は「約 10 分」の形式で書く

---

## 最終チェック

- [ ] 体験ファイルの先頭が `# タイトル｜体験ID` 形式の H1 になっている
- [ ] 相対リンクが壊れていない
- [ ] 機密情報・実在の顧客情報が含まれていない
- [ ] プライバシー上の注意（自分の業務データを使う演習）が明記されている
- [ ] プログラムに追加した場合、`README.md` と `index.md` で体験の並びと必須・選択・発展の区分が一致している
- [ ] 特定のサンプルデータ名を、それを使う体験・プログラム以外の場所に書いていない
- [ ] GitHub Pages の表示が崩れていないことを確認した
